import { FC } from "react";
import { MainContainer, Title } from "components";

const Loader: FC = () => {
  return (
    <MainContainer>
      <Title>Loading...</Title>
    </MainContainer>
  );
};

export default Loader;
